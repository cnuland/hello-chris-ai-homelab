apiVersion: v1
kind: Service
metadata:
  name: claude-code-agent
  namespace: home-assistant
  labels:
    app.kubernetes.io/name: claude-code-agent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: claude-code-agent
  ports:
    - name: http
      port: 8010
      targetPort: http
      protocol: TCP
