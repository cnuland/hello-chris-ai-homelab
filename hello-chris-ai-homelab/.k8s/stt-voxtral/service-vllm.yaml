apiVersion: v1
kind: Service
metadata:
  name: vllm-voxtral
  namespace: voice
  labels:
    app.kubernetes.io/name: vllm-voxtral
    app.kubernetes.io/part-of: voice-assistant
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vllm-voxtral
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
