apiVersion: v1
kind: Service
metadata:
  name: wyoming-voxtral
  namespace: voice
  labels:
    app.kubernetes.io/name: wyoming-voxtral
    app.kubernetes.io/part-of: voice-assistant
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: wyoming-voxtral
  ports:
    - name: wyoming
      port: 10300
      targetPort: wyoming
      protocol: TCP
